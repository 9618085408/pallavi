#include<iostream>
using namespace std;
class EB
{
	char name[20],t;
	int no,cmr,pmr;
	public:
	void calculate;
	};
	void EB::calculate
	{
		switch(+)
		case 'D'
		u=cmr-pmr;
		if(u<=100)
		amt:u*1;
		else if(u>100 & u<=201)
		amt=u*250;
		else(u 201 to 500)
		a=u*4;
		else
		a=u*6;
	}
	return 0;
}
